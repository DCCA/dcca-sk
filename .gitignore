.DS_Store
Thumbs.db
*.swp
*~

# Config preenchida de cada skill (dados reais da empresa nunca vao para o repo).
# Versionamos so o config.example.md; o config.md fica local.
skills/**/config.md

# Config portatil (dotfiles/): NUNCA versionar segredo nem estado local de tool.
dotfiles/*/.credentials.json
dotfiles/*/auth.json
dotfiles/*/settings.local.json
dotfiles/*/history.jsonl

# Artefatos locais de ferramentas de agentes se algum comando for executado
# neste checkout. Runtime e instalacao sao responsabilidade do dcca-env.
.agents/
skills-lock.json
.claude/

# Log solto de ferramenta (excalidraw etc).
*.log
# ...menos as fixtures de eval: o build.log FAZ PARTE do cenario (e a evidencia
# que a regra "evidence before claims" tem que ler em vez da nota velha).
!evals/*/fixture/*.log

# Saida transitoria das evals automatizadas (transcripts, veredito, log da rodada).
# O placar duravel vai pro evals/RESULTS.md, igual as evals de skill.
evals/*/runs/
evals/*/results.json
__pycache__/
